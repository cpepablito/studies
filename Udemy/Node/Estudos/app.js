//  var consume = require('./consumerGroup').consume;
 var publish = require('./producer').publish;
 var consume = require('./consumer').consume;

consume();
// var topic = 'teste';
// var message = 'oi';
// publish(topic, message);
